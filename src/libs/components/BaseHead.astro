---
// Import the global.css file here so that it is included on
// all pages through the use of the <BaseHead /> component.
import { ViewTransitions } from 'astro:transitions'

import type { SiteMeta } from '@/types'
import { siteConfig } from '@/configs/site'

import '@fontsource-variable/plus-jakarta-sans'
import 'virtual:uno.css'
import '@/app.css'

type Props = SiteMeta

const { articleDate, description, ogImage, title, index } = Astro.props

const titleSeparator = '•'
const siteTitle = `${title} ${titleSeparator} ${siteConfig.title}`
const canonicalURL = new URL(Astro.url.pathname, Astro.site)
const socialImageURL = new URL(ogImage ? ogImage : '/favicon.png', Astro.url).href
---

<meta charset='utf-8' />
<meta content='width=device-width, initial-scale=1.0, shrink-to-fit=no' name='viewport' />
<meta content='IE=edge' http-equiv='X-UA-Compatible' />
<meta name='robots' content={`${index ? 'index' : 'noindex'}, follow`} />
<link rel="icon" type="image/png" sizes="32x32" href="/favicon.png" />
<link rel="apple-touch-icon" sizes="180x180" href="/favicon.png" />

<title>{siteTitle}</title>

{/* Canonical URL */}
<link rel='canonical' href={canonicalURL} />

{/* Primary Meta Stacks */}
<meta content={siteTitle} name='title' />
<meta content={description} name='description' />
<meta content={siteConfig.author} name='author' />

{/* Theme Colour */}
<meta content='#fed7aa' name='theme-color' />

{/* Open Graph / Facebook */}
<meta content={articleDate ? 'article' : 'website'} property='og:type' />
<meta content={title} property='og:title' />
<meta content={description} property='og:description' />
<meta content={canonicalURL} property='og:url' />
<meta content={siteConfig.title} property='og:site_name' />
<meta content={siteConfig.ogLocale} property='og:locale' />
<meta content={socialImageURL} property='og:image' />
<meta content='1200' property='og:image:width' />
<meta content='630' property='og:image:height' />
{
	articleDate && (
		<>
			<meta content={siteConfig.author} property='article:author' />
			<meta content={articleDate} property='article:published_time' />
		</>
	)
}

{/* Twitter */}
<meta content='summary_large_image' property='twitter:card' />
<meta content={canonicalURL} property='twitter:url' />
<meta content={title} property='twitter:title' />
<meta content={description} property='twitter:description' />
<meta content={socialImageURL} property='twitter:image' />

{/* RSS auto-discovery */}
<link href='/rss.xml' rel='alternate' title={siteConfig.title} type='application/rss+xml' />

<meta content={Astro.generator} name='generator' />

<meta name="google-site-verification" content={import.meta.env.GOOGLE_CONSOLE_CODE} />
<link rel="sitemap" href="/sitemap-index.xml" />

<ViewTransitions />

<style is:global>
	[class*='art_photo'] {
		animation: slow_pulse 4s infinite;
	}

	@keyframes slow_pulse {
		0%,
		100% {
			opacity: 1;
		}
		50% {
			opacity: 0;
		}
	}
</style>
